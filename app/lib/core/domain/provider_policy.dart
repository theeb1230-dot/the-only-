class ProviderPolicy {
  const ProviderPolicy({this.enabled = true, this.allowSearch = true, this.allowPlayback = true});
  final bool enabled;
  final bool allowSearch;
  final bool allowPlayback;
}
