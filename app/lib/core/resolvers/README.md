# Resolver boundary

Resolvers accept supported URIs and emit normalized `StreamSource` values. They do not navigate, render UI, or mutate library state. Archived resolver implementations remain reference-only until their licenses are explicitly cleared.
