# Provider boundary

Runtime provider implementations must conform to `MediaProvider`, return normalized domain objects, honor URL/security policy, and remain independent from UI/navigation. Archived provider code cannot be copied here until its license row is explicitly CLEARED.
