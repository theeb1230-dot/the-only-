# The Only app

Unified Flutter application shell.

## Verify
```sh
flutter pub get
flutter analyze
flutter test
```

The source archives in the repository root are not runtime dependencies and are not bundled into the application.
